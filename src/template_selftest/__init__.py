"""Template self-test package.

This package exists only so the template's own CI has something to check.
It is not the deliverable. Delete it when copying the template into a real
project. The deliverable is the files around it: AGENTS.md,
.agents/skills/, .github/workflows/, .pre-commit-config.yaml.

Note: `mypy --strict` passes trivially against this docstring-only module.
Type checking does not become meaningful until real code lands here.
"""
